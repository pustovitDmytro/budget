---
output:
  beamer_presentation:
    fig_crop: FALSE
    toc: true
    slide_level: 2
---

```{r setup, include=FALSE}
source("../lib/lib.R")
library(knitr)
opts_chunk$set(echo = FALSE, fig.width = 15, fig.height = 12)
library(xtable)
bold <- function(x) {paste('{\\textbf{',x,'}}', sep ='')}
```

---
title: "`r format(LAST_DATE, '%B %Y')`"
---

# Appendix

```{r, echo=F, results = "asis", message=FALSE, warning=FALSE}
for(year in STORED_YEARS){
   table<-appendixHoldingsYearTable(year)
   if(nrow(table)>0){
   cat("\n\n##", year, " Holdings\n")
   print(xtable(table), scalebox='0.35', comment=FALSE, sanitize.colnames.function=bold, sanitize.rownames.function=bold)
   }
   #cat("\n\n##", year, " Flows\n")
   #print(xtable(appendixFlowsYearTable(year)), scalebox='0.4', comment=FALSE, sanitize.colnames.function=bold, sanitize.rownames.function=bold)
   #cat("\n\n##", year, " Investments\n")
   #print(xtable(appendixInvestmentsYearTable(year)), scalebox='0.4', comment=FALSE, sanitize.colnames.function=bold, sanitize.rownames.function=bold)
}
```